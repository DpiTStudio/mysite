# Настройка VS Code для работы с проектом

## ✅ Виртуальное окружение создано!

Виртуальное окружение создано в папке `venv/` и все зависимости установлены.

## 🔧 Настройка VS Code

### Шаг 1: Выберите интерпретатор Python

1. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
2. Введите: `Python: Select Interpreter`
3. Выберите: `.\venv\Scripts\python.exe` (или `./venv/bin/python` на Linux/Mac)

### Шаг 2: Перезапустите VS Code

После выбора интерпретатора перезапустите VS Code, чтобы изменения вступили в силу.

### Шаг 3: Проверка

Откройте терминал в VS Code (`Ctrl+`` ` или `View > Terminal`) и выполните:

```bash
python manage.py check
```

Если вы видите "System check identified no issues", значит всё настроено правильно!

## 🚀 Запуск проекта

### В терминале VS Code:

```bash
# Активируйте виртуальное окружение (если нужно)
.\venv\Scripts\activate

# Запустите сервер
python manage.py runserver
```

### Или используйте встроенный отладчик:

1. Нажмите `F5` или перейдите в `Run > Start Debugging`
2. Выберите конфигурацию "Python Debugger: Django"
3. Сервер запустится на порту 4234

## 📝 Примечания

- Файл `.vscode/settings.json` уже настроен для использования виртуального окружения
- Если VS Code не видит Django, убедитесь, что выбран правильный интерпретатор
- Все зависимости установлены в виртуальное окружение `venv/`

## ✅ Готово!

Теперь VS Code должен правильно работать с Django проектом.

